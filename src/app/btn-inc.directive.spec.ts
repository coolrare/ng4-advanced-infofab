import { BtnIncDirective } from './btn-inc.directive';

describe('BtnIncDirective', () => {
  it('should create an instance', () => {
    const directive = new BtnIncDirective();
    expect(directive).toBeTruthy();
  });
});
